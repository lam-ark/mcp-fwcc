---
id: "cc_core_lib:BonusGameDirector:api:properties"
title: "BonusGameDirector Detailed Properties & Schema"
category: "cc_core_lib"
tags: ["BonusGameDirector", "bonus_game_director", "cc_core_lib", "properties", "schema", "fields"]
---

# 📋 `BonusGameDirector` Detailed Properties & Schema

---

## 1. Property Schema & Functional Roles

| Property | Type | Description |
| :--- | :--- | :--- |
| **`game`** | `Game, gameMode: any)` | Operational field tracking state in `BonusGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `BonusGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `BonusGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `BonusGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `BonusGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `BonusGameDirector`. |

---

## 2. State Mutation Lifecycle

All state fields in `BonusGameDirector` are initialized during instantiation/registration and mutated during explicit lifecycle method calls. Direct property mutation outside defined methods is strictly discouraged.
