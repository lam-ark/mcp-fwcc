---
id: "cc_core_lib:NormalGameDirector:api:properties"
title: "NormalGameDirector Detailed Properties & Schema"
category: "cc_core_lib"
tags: ["NormalGameDirector", "normal_game_director", "cc_core_lib", "properties", "schema", "fields"]
---

# 📋 `NormalGameDirector` Detailed Properties & Schema

---

## 1. Property Schema & Functional Roles

| Property | Type | Description |
| :--- | :--- | :--- |
| **`_writer`** | `NormalGameWriter` | Operational field tracking state in `NormalGameDirector`. |
| **`game`** | `Game, gameMode: any)` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`currentBetData`** | `string): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<any>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `NormalGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `NormalGameDirector`. |

---

## 2. State Mutation Guardrails

All state fields in `NormalGameDirector` must be initialized during instance registration and modified strictly via designated lifecycle methods. Direct mutation from outside modules is strongly discouraged to maintain state predictability.
