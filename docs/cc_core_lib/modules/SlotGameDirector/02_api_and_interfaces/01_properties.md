---
id: "cc_core_lib:SlotGameDirector:api:properties"
title: "SlotGameDirector Detailed Properties & Schema"
category: "cc_core_lib"
tags: ["SlotGameDirector", "slot_game_director", "cc_core_lib", "properties", "schema", "fields"]
---

# 📋 `SlotGameDirector` Detailed Properties & Schema

---

## 1. Property Schema & Functional Roles

| Property | Type | Description |
| :--- | :--- | :--- |
| **`gameMode`** | `any` | Operational field tracking state in `SlotGameDirector`. |
| **`commandBridge`** | `any` | Operational field tracking state in `SlotGameDirector`. |
| **`game`** | `Game, gameMode: any)` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): void` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `SlotGameDirector`. |
| **`time`** | `number): Promise<void>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<void>` | Operational field tracking state in `SlotGameDirector`. |
| **`data`** | `any): Promise<boolean>` | Operational field tracking state in `SlotGameDirector`. |

---

## 2. State Mutation Lifecycle

All state fields in `SlotGameDirector` are initialized during instantiation/registration and mutated during explicit lifecycle method calls. Direct property mutation outside defined methods is strictly discouraged.
